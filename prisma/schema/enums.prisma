enum Role {
    ADMIN
    VENDOR
    CUSTOMER
}

enum UserStatus {
    ACTIVE
    INACTIVE
    // BANNED
}

enum OrderStatus {
    PENDING
    PAID
    SHIPPED
    CANCELLED
}

enum VendorStatus {
    PENDING
    APPROVED
    REJECTED
}
